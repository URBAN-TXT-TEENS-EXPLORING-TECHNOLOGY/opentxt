export default function Home() {
  return (
    <main style={{ "font-family": "system-ui, sans-serif", padding: "2rem" }}>
      <h1>opentxt</h1>
      <p>
        Effect-native chat + voice backend. The product surface is the Expo app
        (<code>opentxt/app</code>); this server exposes the API under <code>/api/*</code>.
      </p>
    </main>
  )
}
