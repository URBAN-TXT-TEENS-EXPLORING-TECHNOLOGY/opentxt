# LiveKit server the worker registers with (same project the API server mints tokens for)
LIVEKIT_URL=
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# OpenAI Realtime (speech-to-speech model inside the agent session)
OPENAI_API_KEY=

# Gemini Live (only needed when a session requests voiceProvider=google).
# Either name works; GEMINI_API_KEY is what Google AI Studio hands out.
# GOOGLE_API_KEY=
# GEMINI_API_KEY=

# --- optional overrides (keep in sync with opentxt/server) ---
# OPENAI_REALTIME_MODEL=gpt-realtime-2.1
# OPENAI_REALTIME_VOICE=marin
# GOOGLE_REALTIME_MODEL=gemini-2.5-flash-native-audio-preview-12-2025
